## Scripts

Repository release automation is handled by the `release-please` job in [hooks-ci.yml](../.github/workflows/hooks-ci.yml), not by local scripts.

This folder keeps repository utility scripts such as OpenAPI generation.
